# NotesDesk 开发 Todo

## 已完成

- [x] 需求与架构文档
- [x] Swift Package 项目骨架
- [x] `NotesReader`：AppleScript 列出文件夹、读取默认排序第一条
- [x] `NotesWatcher`：FSEvents 监听 Notes 数据目录 + 防抖 0.5s
- [x] `PanelViewModel`：状态管理与自动刷新
- [x] `FloatingPanelController` + `FloatingPanelView`：桌面浮窗
- [x] `WindowDragLayer`：AppKit 透明拖动层（`normalWindow - 1` 层级可拖动）
- [x] `MenuBarExtra` 菜单栏（note.text）：选文件夹、显隐、刷新、设置、退出
- [x] `NotesOpener`：双击浮窗打开系统备忘录
- [x] `AppSettings`：文件夹、透明度、字号、窗口位置持久化
- [x] 仓库级 [开发文档](../../docs/开发文档.md)

## 待本机验收

- [ ] 编译运行：`./run.sh`
- [ ] 自动化权限 → 选文件夹 → 验证内容与备忘录 App 第一条一致
- [ ] 修改备忘录内容，确认浮窗约 1 秒内自动更新
- [ ] 拖动标题栏移动浮窗；双击打开备忘录

## 可选后续（非 MVP）

- [ ] 文件夹重名时 UI 支持选择账户（iCloud / 我的 Mac）
- [ ] 打包为 `.app`（Xcode 工程 + `LSUIElement`）
- [ ] 登录项 / 开机自启动
