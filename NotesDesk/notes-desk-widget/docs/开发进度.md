# NotesDesk Widget Todo

## 已完成

- [x] 需求与架构文档
- [x] NotesWidgetCore 共享模块
- [x] NotesWidgetHost 菜单栏同步应用
- [x] WidgetExtension（StaticConfiguration，跟随 Host 文件夹）
- [x] App Group + 签名配置（XcodeGen `project.yml`）
- [x] localhost 快照服务（`WidgetSnapshotServer`，端口 19876）
- [x] Keychain 共享快照（`KeychainSnapshotStore`）
- [x] Host 5s 轮询 + Widget 10s 时间线兜底
- [x] `debug-check.sh` 环境检查脚本
- [x] 联调验证：Widget 可显示真实备忘录内容

## 待本机 / 可选

- [ ] 登录项：Host 开机自启动
- [ ] 安装到 `/Applications/` 验证生产环境 App Group 行为
- [ ] 缩短/可配置轮询间隔（`AppConstants.hostSyncPollInterval`）

## 说明

- Widget **无法**独立读备忘录；**Host 必须常驻**。这是 WidgetKit 沙盒限制，见 [架构文档](架构.md)。
- `./run-host.sh` 仅适合单独测试 AppleScript，**不能**用于 Widget 联调。
